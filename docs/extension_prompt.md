#### CSS Figma comparison

Prompt:
As a Chrome extension programmer expert, provide the code (manifest version 3) for an interface with a Textarea where the user enters the CSS code from a Figma element (e.g. from a text element font-size, font-weight, font-family, font-color etc., for a box border-color, border-width etc.). 

Then the user clicks a button on the extension interface to select an element on the current website screen that extracts the CSS code of that element (similar to the browser developer tools) so the selected CSS from the current website screen is compared to the Figma CSS code and the comparison is displayed on the browser extension interface so the differences and the matches are highlighted in different colours (green for matches and red for differences).
